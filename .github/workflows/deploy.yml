name: Deploy via SSH

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Add SSH key via agent
        uses: webfactory/ssh-agent@v0.7.0
        with:
          ssh-private-key: |
            ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Setup host verification
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H vh370.timeweb.ru >> ~/.ssh/known_hosts

      - name: Test connection
        run: ssh cx43946@vh370.timeweb.ru 'echo "🔥 Подключение работает!"'



      # - name: Deploy via SSH
      #   env:
      #     SSH_USER: ${{ secrets.SSH_USERNAME }}
      #     SSH_PASS: ${{ secrets.SSH_PASSWORD }}
      #     SSH_HOST: ${{ secrets.SSH_HOST }}
      #     DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
      #   run: |
      #     sshpass -p "$SSH_PASS" ssh -o StrictHostKeyChecking=no "$SSH_USER@$SSH_HOST" <<EOF
      #       cd $DEPLOY_PATH
      #       git pull origin main
      #       composer install --no-interaction --prefer-dist --optimize-autoloader
      #       php yii migrate --interactive=0
      #     EOF
